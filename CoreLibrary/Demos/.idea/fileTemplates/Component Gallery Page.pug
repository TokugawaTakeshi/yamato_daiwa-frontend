//- TODO Correct the relative path 
extends ../../../../node_modules/@yamato-daiwa/frontend/PagesTemplates/RegularWebPage.pug


block append Requirements

  //- TODO Correct the relative paths
  include ../../../../node_modules/@yamato-daiwa/frontend/Components.pug
  include ../../../Galleries/Components/${COMPONENT_NAME}/${COMPONENT_NAME}Gallery.pug


block append Metadata

  -

    /**
     * @param { string } [partialTitle]
     * @returns { string }
     * */
    function generateHTML_Page_Title(partialTitle) {
      return [
        "${COMPONENT_NAME} Component Demos",
        ...partialTitle ? [ ` — ${ partialTitle }` ] : [],
        " — Yamato Daiwa Frontend (YDF)"
      ].join("");
    }

    styleSheetsURIs = [
      "@Partials/YDF-Demos-Primer",
      "@Partials/YDF-Demos-CommonRequirements",
      "@Pages/Components/${COMPONENT_NAME}/${COMPONENT_NAME}GalleryPage" // TODO Correct the path
    ];

    // TODO Correct the path
    scriptsURIs.endOfBody = [ "@CompletePages/Components/${COMPONENT_NAME}/${COMPONENT_NAME}GalleryPage" ];


block append PageContent

  .YDF_Gallery

    +${COMPONENT_NAME}Gallery
