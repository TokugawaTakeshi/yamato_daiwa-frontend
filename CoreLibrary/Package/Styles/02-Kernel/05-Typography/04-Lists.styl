// --- 箇条書き ----------------------------------------------------------------------------------------------------------
UnorderedListDefaultProperties = {
  geometry: {
    fontSize: BASIC_FONT_SIZE_IN_MULTILINE_TEXT_BLOCKS,
    lineHeight: BASIC_LINE_HEIGHT_IN_MULTILINE_TEXT_BLOCKS,
    paddings: { left: 28px },
    verticalSpaceBetweenItems: 8px,
    customDeclarations: {}
  }
}


UnorderedList(preset = UnorderedListDefaultProperties, overridings = {})

  properties = deeplyCloneAndOverrideObject(preset, overridings)
  geometry = properties.geometry


  list-style-type disc
  list-style-position outside

  Paddings(geometry.paddings)

  applyIfNotNull(geometry.customDeclarations)


  li

    MultilineTextWithoutExtraSpaceCausedByLineHeight({
      fontSize: geometry.fontSize,
      lineHeight: geometry.lineHeight
    })


    +li

      margin-top: geometry.verticalSpaceBetweenItems


  ul

    padding-left 10px
    list-style-type circle


    ul

      list-style-type square


// --- 段落番号 ---------------------------------------------------------------------------------------------------------
OrderedListDefaultProperties = {
  geometry: {
    fontSize: BASIC_FONT_SIZE_IN_MULTILINE_TEXT_BLOCKS,
    lineHeight: BASIC_LINE_HEIGHT_IN_MULTILINE_TEXT_BLOCKS,
    paddings: { left: 28px },
    verticalSpaceBetweenItems: 8px,
    customDeclarations: {}
  },
  customDeclarations: {}
}


OrderedList(preset = OrderedListDefaultProperties, overridings = {})

  properties = deeplyCloneAndOverrideObject(preset, overridings)
  geometry = properties.geometry


  list-style-type decimal
  list-style-position outside

  Paddings(geometry.paddings)

  applyIfNotNull(geometry.customDeclarations)


  li

    MultilineTextWithoutExtraSpaceCausedByLineHeight({
      fontSize: geometry.fontSize,
      lineHeight: geometry.lineHeight
    })


    +li

      margin-top: geometry.verticalSpaceBetweenItems


  ol

    padding-left 10px
    list-style-type lower-alpha


    ol

      list-style-type lower-roman
