YDF_Array-isIncludes(targetArray, elementOrPredicate)

  for element in values(targetArray)

    if isFunction(elementOrPredicate) && isTrue(elementOrPredicate(element))

      return true

    else if element == elementOrPredicate

      return true

  return false
