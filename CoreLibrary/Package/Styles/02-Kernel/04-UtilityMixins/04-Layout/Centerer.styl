Centerer(compoundParameter = {}, restParameters__MUST_NOT_BE...)

  validateObjectTypeParameter({
    mixinOrFunctionName: "Centerer",
    targetParameterNumber: 1,
    targetParameter: compoundParameter,
    schema: {
      minimalWidth: {
        type: DataTypes.dimensionalQuantity,
        defaultValue: NARROWEST_SCREEN_WIDTH_UNIT
      },
      maximalWidth: {
        type: DataTypes.dimensionalQuantity,
        defaultValue: 3*NARROWEST_SCREEN_WIDTH_UNIT
      }
    },
    followingParametersWhichMustNotBe: restParameters__MUST_NOT_BE
  })


  width 100%
  min-width: compoundParameter.minimalWidth
  max-width: compoundParameter.maximalWidth

  margin-left auto
  margin-right auto
