/* === Execution
stylus Tests/CompiledCodeCheckOnly/Styles/02-Kernel/02-Functions/04-Quantities/appendUnit.test.styl `
    --out Tests/Build/CompiledCodeCheckOnly/Styles/02-Kernel/02-Functions/04-Quantities/appendUnit.test.css `
    --include Styles
*/


@require "01-Assets.styl"
@require "02-Kernel.styl"


// Simulate the error which was the motivation for the development of the `appendUnit` function
// p(1 + "px")

testForEquality(
  "The unit has been appended",
  appendUnit(1, "px"),
  1px
)

testForEquality(
  "The type of returned value is 'unit'",
  typeof(appendUnit(1, "px")),
  "unit"
)
