/* === Execution
stylus Tests/Source/CompiledCodeCheckOnly/Styles/02-Kernel/03-UtilityMixins/01-Sizing/03-Borders/BordersSizing.test.styl `
    --out Tests/Build/CompiledCodeCheckOnly/Styles/02-Kernel/03-UtilityMixins/01-Sizing/03-Borders/BordersSizing.test.css `
    --include Styles
*/


@require "01-Assets.styl"
@require "02-Kernel.styl"


.BordersTesting

  &-Thickness

    &-All

      BordersSizing({
        thickness: {
          all: 1px
        }
      })


    &-Symmetric

      BordersSizing({
        thickness: {
          horizontalSymmetric: 4px,
          verticalSymmetric: 6px
        }
      })


    &-Separate

      BordersSizing({
        thickness: {
          top: 1px,
          left: 2px,
          right: 4px,
          bottom: 6px
        }
      })


  &-Radius

    &-All

      BordersSizing({
        radius: {
          all: 6px
        }
      })


    &-LeftFillets

      BordersSizing({
        radius: {
          leftFillets: 6px
        }
      })


    &-RightFillets

      BordersSizing({
        radius: {
          rightFillets: 6px
        }
      })


    &-TopFillets

      BordersSizing({
        radius: {
          topFillets: 6px
        }
      })


    &-BottomFillets

      BordersSizing({
        radius: {
          bottomFillets: 6px
        }
      })


    &-Separate

      BordersSizing({
        radius: {
          topLeft: 1px,
          topRight: 2px,
          bottomLeft: 3px,
          bottomRight: 4px
        }
      })
