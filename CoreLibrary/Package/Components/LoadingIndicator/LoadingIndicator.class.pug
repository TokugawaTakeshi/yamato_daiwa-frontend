-

  class LoadingIndicator__YDF {

    static CSS_NAMESPACE = "LoadingIndicator--YDF";

    static Types = {
      variableWidthArcSpinner: "VARIABLE_WIDTH_ARC_SPINNER",
      twoConstantWidthArcsSpinner: "TWO_CONSTANT_WIDTH_ARCS_SPINNER"
    };

    /* ━━━ Themes ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static Themes = { dark: "DARK" };

    static defineThemes(ownAndCorrespondingCompoundControlShellThemes_Names) {
      return ComponentsAuxiliaries.defineThemes(themesNames, LoadingIndicator__YDF);
    }

    static areThemesCSS_ClassesCommon = ComponentsAuxiliaries.areComponentsThemesCommon;

    static considerThemesAsExternal() {
      LoadingIndicator__YDF.areThemesExternal = true;
      return LoadingIndicator__YDF;
    };


    /* ─── Geometry ───────────────────────────────────────────────────────────────────────────────────────────────── */
    static GeometricVariations = {
      regular: "REGULAR",
      small: "SMALL"
    };

    static defineGeometricVariations(geometricVariationsNames) {
      return ComponentsAuxiliaries.defineGeometricVariations(geometricVariationsNames, LoadingIndicator__YDF);
    }

    /* ─── Decoration ─────────────────────────────────────────────────────────────────────────────────────────────── */
    static DecorativeVariations = { regular: "REGULAR" };

    static defineDecorativeVariations(decorativeVariationsNames) {
      return ComponentsAuxiliaries.defineDecorativeVariations(decorativeVariationsNames, LoadingIndicator__YDF);
    }


    /* ━━━ Properties specification ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static propertiesSpecification = {

      type: {
        type: String,
        required: false,
        allowedAlternatives: Object.values(LoadingIndicator__YDF.Types)
      },

      theme: {
        type: String,
        required: false,
        allowedAlternatives: Object.values(LoadingIndicator__YDF.Themes)
      },

      areThemesCSS_ClassesCommon: {
        type: Boolean,
        required: false
      },

      geometricVariation: {
        type: String,
        required: false,
        allowedAlternatives: Object.values(LoadingIndicator__YDF.GeometricVariations)
      },

      decorativeModifiers: {
        type: String,
        required: false,
        allowedAlternatives: Object.values(LoadingIndicator__YDF.DecorativeVariations)
      }

    };

  }
