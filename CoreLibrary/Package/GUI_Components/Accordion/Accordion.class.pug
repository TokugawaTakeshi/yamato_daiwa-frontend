-

  class Accordion__YDF {

    static CSS_NAMESPACE = "Accordion--YDF";


    /* ━━━ Theming ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static Themes = { regular: "REGULAR" };

    static defineThemes(themesNames) {
      return ComponentsAuxiliaries.defineNewThemes(themesNames, Accordion__YDF);
    }

    static areThemesCSS_ClassesCommon = ComponentsAuxiliaries.areComponentsThemesCommon;

    static considerThemesAsCommon() {
      Accordion__YDF.areThemesCSS_ClassesCommon = true;
      return Accordion__YDF;
    }


    /* ─── Geometry ───────────────────────────────────────────────────────────────────────────────────────────────── */
    static GeometricVariations = {
      regular: "REGULAR",
      minimalistic: "MINIMALISTIC"
    };


    /* ─── Decoration ─────────────────────────────────────────────────────────────────────────────────────────────── */
    static DecorativeVariations = {
      regular: "REGULAR",
      minimalistic: "MINIMALISTIC"
    };

    static defineDecorativeVariations(decorativeVariationsNames) {
      return ComponentsAuxiliaries.defineNewDecorativeVariations(decorativeVariationsNames, Button__YDF);
    }


    /* ━━━ Properties Specification ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    static propertiesSpecification = {

      title: {
        type: String,
        required: true,
        minimalCharactersCount: 1
      },

      initiallyExpanded: {
        type: Boolean,
        defaultValue: false
      },

      panelAdditionalCSS_Classes: {
        type: RawObjectDataProcessor.ValuesTypesIDs.oneOf,
        required: false,
        alternatives: [
          {
            type: String,
            minimalCharactersCount: 1
          },
          {
            type: Array,
            element: {
              type: String,
              minimalCharactersCount: 1
            }
          }
        ]
      },

      theme: {
        type: String,
        defaultValue: Accordion__YDF.Themes.regular,
        allowedAlternatives: Object.
            entries(Accordion__YDF.Themes).
            map(([ key, value ]) => ({ key: `Accordion__YDF.Themes.${ key }`, value }))
      },

      areThemesCSS_ClassesCommon: {
        type: Boolean,
        defaultValue: Accordion__YDF.areThemesCSS_ClassesCommon
      },

      geometricVariation: {
        type: String,
        defaultValue: Accordion__YDF.GeometricVariations.regular,
        allowedAlternatives: Object.
            entries(Accordion__YDF.GeometricVariations).
            map(([ key, value ]) => ({ key: `Accordion__YDF.GeometricVariations.${ key }`, value }))
      },

      decorativeVariation: {
        type: String,
        defaultValue: Accordion__YDF.DecorativeVariations.regular,
        allowedAlternatives: Object.
            entries(Accordion__YDF.DecorativeVariations).
            map(([ key, value ]) => ({ key: `Accordion__YDF.DecorativeVariations.${ key }`, value }))
      }

    };


    /* ━━━ Routines ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
    /* ─── ID generating ──────────────────────────────────────────────────────────────────────────────────────────── */
    static counterForID_Generating = 0;

    static generateComponentID() {
      Accordion__YDF.counterForID_Generating++;
      return `ACCORDION--YDF-${ Accordion__YDF.counterForID_Generating }`;
    }

  }
