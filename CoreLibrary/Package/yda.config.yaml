projectBuilding:

  commonSettings:

    selectiveExecutions:

      PugExtensions:
        tasksAndSourceFilesSelection:
          ECMA_ScriptLogicProcessing: [ PugExtensions ]

      StylesAndComponentsTesting:

        tasksAndSourceFilesSelection:
          markupProcessing: [ StylesAndComponentsTesting ]
          stylesProcessing: [ StylesAndComponentsTesting ]
          ECMA_ScriptLogicProcessing: [ ComponentsTesting ]

        browserLiveReloadingSetupID: StylesAndComponentsTesting

      StaticPreviewAnywherePage:
        tasksAndSourceFilesSelection:
          stylesProcessing: [ StaticPreviewAnywherePage ]


  # === Markup =========================================================================================================
  markupProcessing:
    entryPointsGroups:

      StylesAndComponentsTesting:
        topDirectoryRelativePath: Tests
        partialsRecognition:
          excludeFilesWithPrefixes: "_"
          excludeSubdirectoriesWithPrefixes: "_"
        buildingModeDependent:
          LOCAL_DEVELOPMENT:
            outputTopDirectoryRelativePath: Tests/Build


  # === Styles =========================================================================================================
  stylesProcessing:

    entryPointsGroups:

      StylesAndComponentsTesting:

        topDirectoryRelativePath: Tests

        partialsRecognition:
          excludeFilesWithPrefixes: "_"
          excludeSubdirectoriesWithPrefixes: "_"
          excludeSubdirectoriesWithNames: [ "CompiledCodeCheckOnly" ]

        buildingModeDependent:
          LOCAL_DEVELOPMENT:
            outputTopDirectoryRelativePath: Tests/Build

      OutputCheck:

        topDirectoryRelativePath: Tests/Source/OutputCheckOnly

        buildingModeDependent:
          LOCAL_DEVELOPMENT:
            outputTopDirectoryRelativePath: Tests/Build/OutputCheck


      StaticPreviewAnywherePage:

        singleEntryPointRelativePath: PagesTemplates/StaticPreviewAnywherePage/StaticPreviewAnywherePage.styl

        buildingModeDependent:
          LOCAL_DEVELOPMENT:
            outputTopDirectoryRelativePath: PagesTemplates/StaticPreviewAnywherePage


  # === フロントエンド挙動制御記法事前処理 ======================================================================================
  ECMA_ScriptLogicProcessing:

    entryPointsGroups:

      ComponentsTesting:

        topDirectoryRelativePath: Tests/Components
        targetRuntime:
          type: BROWSER

        partialsRecognition:
          excludeFilesWithPrefixes: "_"
          excludeSubdirectoriesWithPrefixes: "_"

        buildingModeDependent:
          LOCAL_DEVELOPMENT:
            outputTopDirectoryRelativePath: Tests/Build/Components

      PugExtensions:

        singleEntryPointRelativePath: Markup/InlineECMAScript/Source/PugExtensions.ts

        targetRuntime:
          type: PUG

        distributing:
          exposingOfExportsFromEntryPoints:
            mustExpose: true
            namespace: __PUG_EXTENSIONS_EXPORTS__

        buildingModeDependent:
          PRODUCTION:
            outputTopDirectoryRelativePath: Markup/InlineECMAScript/Temporary
            revisioning: { disable: true }


  # === Development server =============================================================================================
  browserLiveReloading:

    setups:

      StylesAndComponentsTesting:

        localServer:

          rootDirectoryRelativePath: Tests/Build
          customStartingFileNameWithExtension: Tests.html

        openInBrowsers: chrome
