import React from "react";
import type SVG_IconComponent from "../SVG_IconComponent";


const ${NAME}: SVG_IconComponent = ({ className }: SVG_IconComponent.Properties): React.ReactElement => (
  <svg
    xmlns="http://www.w3.org/2000/svg"
    viewBox=""
    className={ className }
  >
    <path d="" />
  </svg>
);


export default ${NAME};
