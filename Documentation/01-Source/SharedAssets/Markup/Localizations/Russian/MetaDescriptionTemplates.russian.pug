-

  const MetaDescriptionTemplates = {

    pugFunction: ({ functionName, whatFunctionDoes }) =>
        `Функция «${ functionName }» для использования в HTML-препроцессоре «Pug» библиотеки «Yamato Daiwa Frontend» ` +
          `(сокращённо: «YDF») ${ whatFunctionDoes }`,

    stylusFunction: ({ functionName, whatFunctionDoes }) =>
        `Функция «${ functionName }» для использования в CSS-препроцессоре «Stylus» библиотеки «Yamato Daiwa Frontend» ` +
          `(сокращённо: «YDF») ${whatFunctionDoes}`,

    realiseNotes: ({ version }) =>
        "Список изменений (новая функциональность, изменение существующий функциональности и удаления прежней) версии " +
          `${ version } библиотеки для фронтенд веб-разработки «Yamato Daiwa Frontend» (сокращённо: «YDF»).`,

    GUI_Components: {

      AdmonitionBlock:
          "Компонент «AdmonitionBlock» библиотеки «Yamato Daiwa Frontend» (сокращённо: «YDF») и её адаптаций к " +
            "фреймворкам представляет собой рамку с текстом и иногда дополнительным содержимым. " +
          "В графических пользовательский интерфейсах является одним из способов оповещения пользователей с " +
            "возможностью предложить отреагировать на сообщение с помощью кнопок. " +
          "В типографике, данный компонент используется для выделения блоков текста разной степени важности.",

      Badge:
          "В «Yamato Daiwa Frontend», «Badge» является неинтерактивным компонентом для отображения важных метаданных, " +
            "например категории товара, даты и времени события и так далее.",

      buttons: {

        plain:
          "Компонент «Button» библиотеки «Yamato Daiwa Frontend» (сокращённо: «YDF») и её адаптаций к фреймворкам " +
            "одним и самых популярных элементов управления, как-либо реагирующим на клик мыши или касание на сенсорном " +
            "экране. " +
          "Несмотря на кажущуюся простоту, кнопка является довольно сложным элементом как с точки зрения разметки, " +
            "так и стилей, однако YDF, насколько возможно, инкапсулирует эту сложность."

      }

    }

  };
