extends ../../../../../../../node_modules/@yamato-daiwa/programming_documentation/Layouts/TwoColumns/TwoColumnsLayout.pug


block append Requirements

  include ../../../../../../SharedAssets/Markup/SharedAssets.russian.pug
  include ../../../../../../SharedComponents/SharedComponents.russian.pug


block append Metadata

  -

    HTML_PAGE_LANGUAGE = "ru";
    HTML_PAGE_TITLE = buildHTML_PageTitle("ValueCheckers");
    HTML_PageMetaData = {
      description: "",
      author: HTML_PAGE_AUTHOR
    };

    FAVICON_URI = "@MustBeInPublicRoot/favicon";

    styleSheetsURIs = [ "@Shared/CommonStyles", "@Layouts/TwoColumns/TwoColumnsLayout", "@Shared/ExternalComponents" ];
    scriptsURIs.endOfBody = [ "$CommonLogic" ];

    RoutingEmulator.setCurrentRoute("coreLibrary.styles.kernel.functions.typeCheckers");

    LanguageDropDownList__YDF_DK.setLinks({
      english: "./TypeCheckers.english.html",
      japanese: "./TypeCheckers.japanese.html"
    });


block append SpecificContent

  article

    -
      const sectioning = localizedRouting.coreLibrary.$children.
          styles.$children.
          kernel.$children.
          functions.$children.
          typeCheckers.$sectioning;

    h1.Article-Heading1 Проверка типов значений

    //- ━━━ Строки ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    h2.Article-Heading2(
      id= sectioning.strings.$anchor
    )= sectioning.strings.$tableOfContentsTitle

    //- ─── isEmptyString ──────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.strings.$sectioning.isEmptyString.$anchor
    )= sectioning.strings.$sectioning.isEmptyString.$tableOfContentsTitle

    //- ─── isNonEmptyString ───────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.strings.$sectioning.isNonEmptyString.$anchor
    )= sectioning.strings.$sectioning.isNonEmptyString.$tableOfContentsTitle

    //- ─── isString ───────────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.strings.$sectioning.isString.$anchor
    )= sectioning.strings.$sectioning.isString.$tableOfContentsTitle


    //- ━━━ Величины ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    h2.Article-Heading2(
      id= sectioning.quantities.$anchor
    )= sectioning.quantities.$tableOfContentsTitle

    //- ─── isDimensionalOrDimensionlessQuantity ───────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.quantities.$sectioning.isDimensionalOrDimensionlessQuantity.$anchor
    )= sectioning.quantities.$sectioning.isDimensionalOrDimensionlessQuantity.$tableOfContentsTitle

    //- ─── isDimensionalQuantity ──────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.quantities.$sectioning.isDimensionalQuantity.$anchor
    )= sectioning.quantities.$sectioning.isDimensionalQuantity.$tableOfContentsTitle

    //- ─── isDimensionlessQuantity ────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.quantities.$sectioning.isDimensionlessQuantity.$anchor
    )= sectioning.quantities.$sectioning.isDimensionlessQuantity.$tableOfContentsTitle

    //- ─── isNaturalNumber ────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.quantities.$sectioning.isNaturalNumber.$anchor
    )= sectioning.quantities.$sectioning.isNaturalNumber.$tableOfContentsTitle


    //- ━━━ Булевские значения ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    h2.Article-Heading2(
      id= sectioning.booleans.$anchor
    )= sectioning.booleans.$tableOfContentsTitle

    //- ─── isBoolean ──────────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.strings.$sectioning.isBoolean.$anchor
    )= sectioning.strings.$sectioning.isBoolean.$tableOfContentsTitle

    //- ─── isFalse ────────────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.strings.$sectioning.isFalse.$anchor
    )= sectioning.strings.$sectioning.isFalse.$tableOfContentsTitle

    //- ─── isTrue ─────────────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.strings.$sectioning.isTrue.$anchor
    )= sectioning.strings.$sectioning.isTrue.$tableOfContentsTitle


    //- ━━━ Пустые значения ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    h2.Article-Heading2(
      id= sectioning.nullables.$anchor
    )= sectioning.nullables.$tableOfContentsTitle

    //- ─── isNull ─────────────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.nullables.$sectioning.isNull.$anchor
    )= sectioning.nullables.$sectioning.isNull.$tableOfContentsTitle

    //- ─── isFalse ────────────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.nullables.$sectioning.isFalse.$anchor
    )= sectioning.nullables.$sectioning.isFalse.$tableOfContentsTitle


    //- ━━━ Другие ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    h2.Article-Heading2(
      id= sectioning.others.$anchor
    )= sectioning.others.$tableOfContentsTitle

    //- ─── isCalcExpression ───────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.others.$sectioning.isCalcExpression.$anchor
    )= sectioning.others.$sectioning.isCalcExpression.$tableOfContentsTitle

    //- ─── isFunction ─────────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.others.$sectioning.isFunction.$anchor
    )= sectioning.others.$sectioning.isFunction.$tableOfContentsTitle

    //- ─── isIdentifier ───────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.others.$sectioning.isIdentifier.$anchor
    )= sectioning.others.$sectioning.isIdentifier.$tableOfContentsTitle

    //- ─── isObject ───────────────────────────────────────────────────────────────────────────────────────────────────
    h3.Article-Heading3(
      id= sectioning.others.$sectioning.isObject.$anchor
    )= sectioning.others.$sectioning.isObject.$tableOfContentsTitle
